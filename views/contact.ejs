<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
    <%- include('partials/head') %>
  </head>
  <header><%- include('./partials/nav') %></header>
  <body>
    <div class="container mp-5 bg-white p-5 rounded shadow my-5 contact-form">
      <h1 class="mb-4 text-center">Contact Us</h1>
      <form action="/contact" method="POST">
        <div class="mb-3">
          <label for="name" class="form-label"
            >Name & Surname <span class="required">(Required)</span></label
          >
          <input
            type="text"
            class="form-control"
            name="name"
            placeholder="Name"
            required
          />
        </div>
        <div class="mb-3">
          <label for="email" class="form-label"
            >Email <span class="required">(Required)</span></label
          >
          <input
            type="email"
            class="form-control"
            name="email"
            placeholder="Email"
            required
          />
        </div>
        <div class="mb-3">
          <label for="message" class="form-label"
            >Your Message <span class="required">(Required)</span></label
          >
          <textarea
            class="form-control"
            name="message"
            placeholder="Your message"
            id="message"
            rows="5"
            required
          ></textarea>
        </div>
        <div class="d-grid">
          <button type="submit" class="btn btn-success contact-button">
            Send
          </button>
        </div>
      </form>
    </div>
  </body>
  <footer><%- include('./partials/footer') %></footer>
</html>
